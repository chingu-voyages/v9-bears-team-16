{
  "name": "v9-bears-16-server",
  "scripts": {
    "open": "./node_modules/.bin/open-cli http://localhost:4000",
    "dev": "node src/index.js",
    "deploy": "prisma deploy",
    "start": "npm run deploy && npm  run dev",
    "prisma": "prisma"
  },
  "dependencies": {
    "graphql-yoga": "1.17.4",
    "prisma-client-lib": "1.34.0"
  },
  "devDependencies": {
    "open-cli": "^5.0.0",
    "prisma": "^1.34.0"
  }
}
