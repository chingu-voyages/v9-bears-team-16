{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "scripts": {
    "generate": "lerna run generate --parallel",
    "start": "lerna run start --parallel"
  }
}
